import styled from 'styled-components';

const Container = styled.div`
  width: 90%;
  height: 80%;
  margin: 0 auto;
  background-color: #fff;
`;

const NewTaskPanel = () => {
  return <Container>new task panel</Container>;
};

export default NewTaskPanel;
